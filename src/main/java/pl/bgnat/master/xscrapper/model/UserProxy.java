package pl.bgnat.master.xscrapper.model;

public record UserProxy(String host,
                        String port,
                        String username,
                        String password) {
}
