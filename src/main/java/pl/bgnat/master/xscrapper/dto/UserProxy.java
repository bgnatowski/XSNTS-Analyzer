package pl.bgnat.master.xscrapper.dto;

public record UserProxy(String host,
                        String port,
                        String username,
                        String password) {
}
